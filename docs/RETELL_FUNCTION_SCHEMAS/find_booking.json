{
  "type": "object",
  "properties": {
    "customer_phone": {
      "type": "string",
      "minLength": 1,
      "description": "Customer phone number to search for existing confirmed bookings."
    },
    "customer_name": {
      "type": "string",
      "description": "Optional customer name hint used to narrow matching bookings."
    },
    "date": {
      "type": "string",
      "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
      "description": "Optional booking date filter in YYYY-MM-DD format."
    },
    "time": {
      "type": "string",
      "pattern": "^\\d{2}:\\d{2}$",
      "description": "Optional booking time filter in HH:MM (24-hour) format."
    },
    "lookahead_days": {
      "type": "integer",
      "minimum": 1,
      "maximum": 365,
      "description": "Optional future search window in days (default 30)."
    }
  },
  "required": [
    "customer_phone"
  ],
  "additionalProperties": false
}
